# Agent Instructions — denotecli

## 이 리포가 무엇인가

3,000개가 넘는 Denote/org-mode 노트를 에이전트가 읽는 **문**이다. Go stdlib만 쓰고,
단일 바이너리이며, 출력은 전부 JSON이다. 소스는 `denotecli/` 한 패키지, 진입점은
`./run.sh` (`build` / `test` / `showcase` / `cover` / `bench`).

이 문이 **무엇을 통과시키지 않는가**가 계 전체의 눈을 정한다. 유도 가능한 사실을
여기서 안 주면 호출자가 org 파일을 직접 열어 자기 파서를 들고, 그 사본은 언젠가
원본과 갈라진다. 실제로 그랬다 — 아래 § 두 모양의 도장.

## 담당자 문서 — id가 닻이고 제목은 닻이 아니다

```text
담당자 문서 = denote:20260222T090000
읽는 자리  = ~/org/botlog/20260222T090000--*.org   (org 원본)
```

**id만 안 움직인다.** 2026-09-04 실측: 이 노트가 개명되면서 제목·슬러그·`#+filetags:`·
파일명이 전부 바뀌었고 `#+identifier:`만 그대로였다. 그러니 "이 리포의 담당자 문서가
어느 것인가"를 제목이나 `§denotecli` 검색으로 **다시 유도하지 마라.** 위의 id를 읽어라.
오늘의 제목은 `§denotecli: #담당자 day-query 설계 검토 통합 타임라인 스펙`이지만,
이 줄과 파일이 어긋나면 **파일이 이긴다.**

**`~/org/botlog`의 org 원본을 읽어라.** `notes/content/`의 내보내진 md는 한 주기
늦어서 갓 고친 노트가 거기서는 없거나 낡은 것으로 읽힌다. 판정 근거가 아니다.
`denotecli read 20260222T090000 --outline` 을 먼저 부르고 필요한 절만 열어라.

의미 있는 설계·경계 변경 뒤에는 그 방을 고쳐라 (히스토리 + 현재 보고). 새 botlog을
만들지 않는다. **`#+hugo_lastmod:` 는 담당자의 손도장이다** — 내용이 정말 바뀌었을
때만 찍는다(`agent-denote-set-front-matter`). 히스토리 한 줄은 로그이지 수정이
아니므로 기준선을 올리지 않는다 (GLG 판정 2026-09-04). 세션 핸드오프는 `NEXT.md`.

**`~/org` 은 커밋하지 않는다.** 노트는 고치되 커밋은 GLG와 org 레인의 손이다.

## 출력 계약 — 유도 가능한 것은 이쪽이 준다

### 두 모양의 도장

`date` 는 **생성** 시각이고 수정 시각이 아니다. 수정은 `#+hugo_lastmod:` 하나뿐이며
`search` / `list` / `day` / `read` 가 전부 두 모양으로 함께 싣는다:

| 필드 | 값 | 쓰는 자리 |
|---|---|---|
| `lastmod` | `2026-05-18` | 날짜 비교 (`day` 커맨드와 같은 모양) |
| `hugo_lastmod` | `[2026-05-18 Mon 09:09]` | 원본 그대로 — **HH:MM이 필요한 비교** |

시각을 버리면 안 되는 이유가 실측으로 섰다. `sorge` 의 순회는 「담당자 문서가 리포보다
뒤처졌는가」를 이 도장 하나로 판정하는데, 필드를 못 얻어 `sweep.py` 가 org 파일을 직접
정규식으로 팠고, 날짜만 잡고 시각을 버려서 **같은 날 21:55 도장보다 이른 18:32 커밋을
빚으로 셌다**(없는 빚 1건, 2026-09-04). 사본이 원본과 갈라진 것이다.

> **규율: 유도 가능한 것을 호출자가 다시 파게 만들지 마라.** 파일에 있는데 JSON에 없으면
> 그 필드는 언젠가 남의 리포 안에서 틀린 정규식으로 부활한다.

### `abstract`

`read` 는 (일반·`--outline` 둘 다) 첫 헤딩 앞의 콜아웃 인용 블록을 구조화해 싣는다:
`{kind, title, body}`. `[!abstract] 이 노트에 대하여` 와 `#+description:` 이 그 노트가
**무엇인지** 말하는 유일한 자리이므로, 첫 호출에 보여야 한다. 평범한 인용은 abstract가
아니고, 첫 헤딩 아래의 콜아웃은 본문이지 노트의 abstract가 아니다.

## 손대는 법

- 필드를 더할 때는 **추가만** 한다. 기존 소비자(`day`, `sorge/sweep.py`, `agent-config`의
  `denotecli` 스킬)가 읽는 키의 의미를 바꾸지 않는다. 옵셔널 필드는 `omitempty`.
- 출력 계약을 바꾸면 **코드를 고친 뒤에 문서를 고친다.** 아직 서지 않은 것을 현황 칸에
  적지 않는다.
- 회귀는 테스트로 못 박는다: `json_contract_test.go`(빈 배열·플래그 검증),
  `abstract_test.go`(도장 두 모양·콜아웃 경계).
- `./run.sh test` 가 초록이어야 하고, `./run.sh showcase` 로 실제 검색 패턴 출력을 눈으로 본다.
- 커밋은 `commit` 스킬. 푸시는 GLG가 이 세션에서 명시적으로 요청할 때만.

### `run.sh build <DIR>` — 목적지가 남의 리포이면 멈춰라

`build` 의 `DIR` 은 아무 경로나 받는다. 그래서 2026-09-04 이 리포의 손이
`agent-config/skills/denotecli/` 에 바이너리를 직접 얹었고, **그 리포의 배포 게이트를
지나지 않았다** — 그쪽 `run.sh` 의 `setup:build` 는 미커밋 트리로 빌드하는 것을 거부하고
`.provenance.json` 에 revision·sha를 적는데, 내 손은 그 둘 다 건너뛰었다. 결과는 그 기계가
provenance가 가리키는 것과 다른 바이너리를 실행하는 상태였다(agent-config 담당자
`20260904T122537-cff7e5` 가 재서 알려줬다 — 전달받은 사실, 내가 재지 않음).

**목적지가 다른 리포이면 그 리포의 배포 경로를 먼저 읽는다. 얹는 것은 그 집의 손이다.**
여기서 하는 일은 커밋·푸시까지이고, 배포는 받는 집이 자기 게이트로 한다.

## 곁 리포

- **agent-config** — 에이전트가 읽는 `denotecli` 스킬 문서가 산다. 출력 계약이 바뀌면
  그쪽 스킬 문서도 같이 움직여야 한다.
- **sorge** — 이 리포의 순회 담당. `sweep.py` 가 이 CLI의 소비자다.
